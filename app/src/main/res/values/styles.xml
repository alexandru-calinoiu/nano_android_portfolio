<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:buttonStyle">@style/Nanodegree.Portfolio.Button</item>
    </style>

    <style name="Nanodegree.Portfolio.Button" parent="Widget.AppCompat.Button">
        <item name="android:layout_marginTop">@dimen/button_top_margin</item>
        <item name="android:layout_width">match_parent</item>
    </style>
</resources>
